/**
 * date: 17-8-26
 * create by learnwy
 * licensed is GPL-3.0
 */
import React from 'react';
import ReactDOM from 'react-dom';
import { Button } from 'antd';

const rootEl = document.getElementById('root');

ReactDOM.render(
  <div><Button>按钮</Button></div>, rootEl
);
